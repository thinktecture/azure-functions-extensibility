{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to sqlserver-demo",
      "type": "coreclr",
      "request": "attach",
      "processId": "${command:azureFunctions.pickProcess}"
    }
  ]
}
